
export interface ServiceCardProps {
  title: string;
  description: string;
  icon: React.ReactNode;
}

export interface DifferentialProps {
  title: string;
  description: string;
  icon: React.ReactNode;
}
